#include "FL_ProjectileBreakParticles.h"

// Header-only implementation. This file exists so the project tree mirrors the
// added effect system, but it does not need to be linked into the Xcode target.
